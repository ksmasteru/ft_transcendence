// API Configuration - baked in at build time (see webpack.config.js
// DefinePlugin) from the API_BASE_URL env var, e.g. Railway's public gateway URL.
export const API_BASE_URL = process.env.API_BASE_URL;

